{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "/bin/sh \"${PLUGIN_ROOT}/hooks/session_start_inject.sh\"",
            "statusMessage": "Loading scholarly register constraint"
          }
        ]
      }
    ]
  }
}
